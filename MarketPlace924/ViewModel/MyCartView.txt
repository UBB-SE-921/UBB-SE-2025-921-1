<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MarketPlace924.View.MyCartView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:viewModel="using:MarketPlace924.ViewModel"
    xmlns:domain="using:MarketPlace924.Domain"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <TextBlock Text="My Cart" FontSize="32" FontWeight="Bold" Margin="10" />
        <ListView ItemsSource="{x:Bind ViewModel.CartItems, Mode=OneWay}" Margin="10,50,10,10">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="viewModel:CartItemViewModel">
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <TextBlock Text="{x:Bind Product.Name}" Width="200" />
                        <TextBlock Text="{x:Bind Product.Price, Mode=OneWay}" Width="100" />
                        <TextBox Text="{x:Bind Quantity, Mode=TwoWay}" Width="50" />
                        <Button Content="Remove" Command="{x:Bind ViewModel.RemoveFromCartCommand}" CommandParameter="{x:Bind Product}" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
